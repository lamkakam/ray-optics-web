# `shared/lib/lens-prescription-grid/index.ts`

Barrel exports for the shared lens prescription grid module.

Exports the reusable prescription cell components, shared action wrapper, common column builders, numeric parser, and AG Grid defaults used by the Lens Editor and Optimization prescription grids.
