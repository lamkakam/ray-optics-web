# `features/optimization/components/OptimizationProgressModal/index.ts`

Barrel export for the optimization progress modal component.

Exports `OptimizationProgressModal` so page-level optimization imports can target the component directory instead of the concrete source file.
