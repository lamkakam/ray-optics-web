# `features/optimization/components/OptimizationApplyConfirmModal/index.ts`

Barrel export for the optimization apply confirmation modal component.

Exports `OptimizationApplyConfirmModal` so page-level optimization imports can target the component directory instead of the concrete source file.
