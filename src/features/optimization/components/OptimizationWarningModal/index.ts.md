# `features/optimization/components/OptimizationWarningModal/index.ts`

Barrel export for the optimization warning modal component.

Exports `OptimizationWarningModal` so page-level optimization imports can target the component directory instead of the concrete source file.
