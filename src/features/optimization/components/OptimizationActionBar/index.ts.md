# `features/optimization/components/OptimizationActionBar/index.ts`

Barrel export for the optimization action bar component.

Exports `OptimizationActionBar` so page-level optimization imports can target the component directory instead of the concrete source file.
