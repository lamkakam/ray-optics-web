# `features/optimization/components/AsphereVarModal/index.ts`

Barrel export for the asphere variable/pickup modal component.

Exports `AsphereVarModal` so page-level optimization imports can target the component directory instead of the concrete source file.
