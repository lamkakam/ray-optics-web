# `features/optimization/components/OptimizationOperandsTab/index.ts`

Barrel export for the optimization operands tab component.

Exports `OptimizationOperandsTab` so page-level optimization imports can target the component directory instead of the concrete source file.
