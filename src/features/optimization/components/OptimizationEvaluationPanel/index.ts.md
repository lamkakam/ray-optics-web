# `features/optimization/components/OptimizationEvaluationPanel/index.ts`

Barrel export for the optimization evaluation panel component.

Exports `OptimizationEvaluationPanel` so page-level optimization imports can target the component directory instead of the concrete source file.
