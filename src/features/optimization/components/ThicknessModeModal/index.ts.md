# `features/optimization/components/ThicknessModeModal/index.ts`

Barrel export for the thickness variable/pickup modal component.

Exports `ThicknessModeModal` so page-level optimization imports can target the component directory instead of the concrete source file.
