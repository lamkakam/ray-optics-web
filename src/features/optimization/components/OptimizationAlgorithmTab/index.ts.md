# `features/optimization/components/OptimizationAlgorithmTab/index.ts`

Barrel export for the optimization algorithm tab component.

Exports `OptimizationAlgorithmTab` so page-level optimization imports can target the component directory instead of the concrete source file.
