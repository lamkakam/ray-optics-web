# `features/optimization/components/RadiusModeModal/index.ts`

Barrel export for the radius variable/pickup modal component.

Exports `RadiusModeModal` so page-level optimization imports can target the component directory instead of the concrete source file.
