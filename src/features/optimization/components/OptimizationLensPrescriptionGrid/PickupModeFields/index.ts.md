# `features/optimization/components/LensPrescriptionGrid/PickupModeFields/index.ts`

Barrel export for the shared optimization pickup mode fields component.

Exports `PickupModeFields` so optimization modal components can import from the component directory instead of the concrete source file.
