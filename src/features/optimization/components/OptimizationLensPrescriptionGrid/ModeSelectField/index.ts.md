# `features/optimization/components/LensPrescriptionGrid/ModeSelectField/index.ts`

Barrel export for the shared optimization mode selector component.

Exports `ModeSelectField` so optimization modal components can import from the component directory instead of the concrete source file.
