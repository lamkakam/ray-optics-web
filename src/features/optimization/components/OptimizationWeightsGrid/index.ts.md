# `features/optimization/components/OptimizationWeightsGrid/index.ts`

Barrel export for the optimization weights grid component.

Exports `OptimizationWeightsGrid` so page-level optimization imports can target the component directory instead of the concrete source file.
