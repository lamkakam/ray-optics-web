# `features/optimization/components/LensPrescriptionGrid/OptimizationInspectionModals/index.ts`

Barrel export for the optimization lens prescription inspection modal wrappers.

Exports `OptimizationInspectionModals` for page-level imports that need the inspection modal collection.
