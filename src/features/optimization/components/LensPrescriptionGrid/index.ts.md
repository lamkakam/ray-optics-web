# `features/optimization/components/LensPrescriptionGrid/index.ts`

Narrow public barrel for the optimization lens prescription grid.

Exports `OptimizationLensPrescriptionGrid` and `OptimizationLensPrescriptionGridProps`. Internal helper components such as inspection modals, mode selectors, and pickup fields remain available only through their own directory barrels.
